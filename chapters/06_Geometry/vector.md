`arg(a, b)` 要求 `a`、`b` 均为非零向量，返回从 `a` 到 `b` 的有向夹角，范围为 $[-\pi, \pi]$；正值表示逆时针，负值表示顺时针，$0$ 表示同向，$\pm\pi$ 表示反向。无向最小夹角为 `abs(arg(a, b))`。

`dir(a, b)` 同样要求两向量非零，返回 $1$ 表示同向，$-1$ 表示反向，$0$ 表示不平行。仅判断转向时直接使用 `sgn(a % b)`；`arg` 在反向处存在 $-\pi/\pi$ 断点。
