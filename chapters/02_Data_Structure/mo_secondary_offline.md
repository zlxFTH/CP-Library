当单步转移为 $O(\log n)$，但贡献关于区间可以作前缀差分时，将莫队的连续指针移动再次离线，批量计算每次询问相对上一次询问的变化量。

`move_l`、`move_r` 分别把一整段端点移动压成 $O(1)$ 个 `E`，不要逐点枚举；`calc` 扫描所有事件并累加到 `d[id]`。最后按莫队顺序对变化量求前缀和得到 `ans`。`E` 的字段和三处函数均随题目定义；逆序对中配合值域分块可做到 $O(n\sqrt q+n\sqrt n)$ 时间、$O(n+q)$ 空间。
