64 位异或线性基。`ins(x)` 返回 $x$ 是否与当前基独立，`rk` 为秩；`has(x)` 判断能否表示，`mx(x)` 求 $x$ 异或张成空间元素的最大值，`mn()` 求最小非零值（空基返回 0）。这些操作均为 $O(64)$。

`merge(b)` 合并两个基。`reduce()` 原地化为约化基并按主元从低到高返回；之后 `kth(k)` 返回第 $k$ 小的不同子集异或值（0-index，要求 $0\le k<2^{rk}$）。合并、约化复杂度 $O(64^2)$。
