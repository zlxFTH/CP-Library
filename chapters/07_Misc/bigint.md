非负整数，复用缺省源的 `LL / I / SZ / pb`。`BI a = 123` 或 `BI a("123")`（非空十进制数字串），支持 `cin / cout`、比较和 `+ - *`；减法须保证 `a >= b`。`a / b`、`a % b` 的除数为正 `LL`，`a.div(b)` 同时返回商、余数。不含高精除高精。

十进制逐位存储；加减、比较、除以 `LL` 为线性，乘法为 $O(nm)$，$n,m$ 为十进制位数。
