以下函数均要求向量非零。`half` 将极角按 $[0, \pi)$ 与 $[\pi, 2\pi)$ 分成两半；默认启用精确版，浮点数据需要把坐标轴附近视为同一方向时，可注释精确版并启用 `eps` 版。

`cmp(a, b)` 判断 `a` 的极角是否小于 `b`，可直接用于 `sort`；`cmp_eq(a, b)` 判断两向量是否同向，不比较长度。

`cmp_ct(a, b, c)` 判断圆周上的方向顺序是否为逆时针 `a -> b -> c`：成立返回 $1$，不成立返回 $0$，`a`、`c` 中任一向量与 `b` 同向时返回 $-1$。
